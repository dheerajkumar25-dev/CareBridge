{
  "name": "carebridge",
  "version": "1.0.0",
  "private": true,
  "description": "Doctor-Patient Appointment Portal (MERN stack)",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
